import "../styles.css";
import "@asktro/nextra/dist/index.css"
import { Analytics } from "@vercel/analytics/react";

export default function Nextra({ Component, pageProps }) {
  return (
    <>
      <Component {...pageProps} />
      <Analytics />
    </>
  );
}
